# archive/

Historical material kept for reference but not part of the
authoritative documentation set.

* `docs_path_b_2026-04/` — pre-2026-04 audit reports and the "Path B"
  remediation plan that produced the current implementation. These
  files were previously under `docs/_archive/` but moved here so
  `docs/` contains only the authoritative current documentation that
  reviewers should treat as canonical.

Authoritative docs live in:

* the repository root (`README.md`, `HOW_TO_RUN.md`, `CITATION.cff`)
* `docs/` (`RELEASE.md`, `METHODS_REPRO_APPENDIX.md`,
  `STATISTICAL_METHODS.md`, `CLAIMS_TO_EVIDENCE.md`,
  `MODE_EFF_EMPIRICAL.md`, `PRE_2025_04_FAIRNESS_FIX_DELTA.md`)
