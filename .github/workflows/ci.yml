name: ci

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Cross-platform test suite. README documents Windows + macOS as
  # supported development platforms; the matrix proves it on every
  # push. Current-evidence hash checks, when a schema-v2 manifest actually
  # exists, live in the dedicated `current-evidence-boundary` job below.
  backend-tests:
    name: backend-tests (${{ matrix.os }} / py${{ matrix.python-version }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.11"]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install backend
        run: |
          python -m pip install --upgrade pip
          # Install with the [dev] extra so httpx and pytest-asyncio are
          # available -- TestClient-based integration tests fail without
          # them. The "[dev]" form is a project optional-dependencies
          # group declared in pyproject.toml.
          pip install -e "agribrain/backend[dev]"
      - name: Run backend tests
        run: |
          python -m pytest agribrain/backend/pirag/tests agribrain/backend/tests
      - name: Run stochastic simulation validation test
        run: |
          python mvp/simulation/tests/test_stochastic_quick.py
          python -m pytest mvp/simulation/tests/test_regression_evidence_boundary.py

  publication-evidence-tests:
    name: publication and recovery tests (ubuntu / py3.11)
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e "agribrain/backend[dev]"
      - name: Validate Slurm launcher syntax
        run: |
          bash -n hpc/hpc_run.sh hpc/hpc_sensitivity_run.sh \
            hpc/publication_recovery_run.sh hpc/hpc_publish_recovery.sh \
            hpc/hpc_sensitivity_publish_recovery.sh \
            hpc/hpc_full_submission_recovery.sh
      - name: Run simulation publication and recovery tests
        run: python -m pytest mvp/simulation/tests

  # Slow-marker tests: full end-to-end simulator runs (mode x scenario
  # matrix) that the default `addopts = "-m 'not slow'"` skips. We run
  # them here on Linux/Python 3.11 only -- they are not expected to be
  # platform-sensitive but they are minutes-per-test rather than
  # milliseconds, so the cross-platform matrix above stays fast.
  # The 2026-05 forensic pass surfaced a stale calibration in one slow
  # test that was hidden behind the addopts opt-out; this job ensures
  # future drift fails CI loudly. continue-on-error is intentionally
  # not set: a slow-test failure must red the build.
  slow-tests:
    name: slow-tests (ubuntu / py3.11)
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Install backend (with [dev] extras)
        run: |
          python -m pip install --upgrade pip
          pip install -e "agribrain/backend[dev]"
      - name: Run slow-marked tests
        run: |
          # --override-ini="addopts=" wins over the addopts in
          # agribrain/backend/pyproject.toml ("-m 'not slow'") so the
          # explicit -m slow selector here is honoured. Without the
          # override, the addopts and the -m flag conflict and pytest
          # selects nothing.
          python -m pytest --override-ini="addopts=" -m slow \
            agribrain/backend

  # Evidence boundary for the source candidate. An ordinary branch/main
  # checkout is allowed to contain no current publication evidence; in that
  # case CI says so and does not search archives or legacy directories for a
  # substitute. If an exact schema-v2 manifest is present, the tracked current
  # artifacts are validated. Publication evidence is distributed as a sealed
  # GitHub Release asset tied to the exact publication-source tag, rather than
  # committed after generation (which would change the code identity recorded
  # by the evidence). A source checkout may therefore legitimately contain no
  # current evidence.
  current-evidence-boundary:
    name: current publication-evidence boundary
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Detect exact current-evidence manifest
        id: evidence
        run: |
          python - <<'PY'
          import json
          import os
          import re
          from pathlib import Path

          manifest = Path("mvp/simulation/results/artifact_manifest.json")
          if not manifest.is_file():
              with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as out:
                  out.write("has_current=false\n")
                  out.write("recovery_receipt=\n")
              print(
                  "::notice::Source-only candidate: no current publication "
                  "evidence is committed. Archived/legacy outputs were not used."
              )
              raise SystemExit(0)

          payload = json.loads(manifest.read_text(encoding="utf-8"))
          if payload.get("schema_version") != 2:
              raise SystemExit(
                  "Current evidence manifest exists but schema_version is not 2."
              )
          for key in (
              "git_commit", "simulation_source_commit", "publication_code_commit",
          ):
              if not re.fullmatch(r"[0-9a-f]{40}", str(payload.get(key, ""))):
                  raise SystemExit(f"Current manifest has invalid {key}.")
          if not isinstance(payload.get("artifacts"), list) or not payload["artifacts"]:
              raise SystemExit("Current manifest has no artifact inventory.")
          recovery_receipt = ""
          if payload.get("dual_provenance") is True:
              authorization = payload.get("recovery_authorization")
              if not isinstance(authorization, dict):
                  raise SystemExit(
                      "Dual-provenance manifest lacks recovery_authorization."
                  )
              relative = str(authorization.get("receipt_file", ""))
              candidate = Path(relative)
              if (
                  not relative
                  or candidate.is_absolute()
                  or "\\" in relative
                  or any(part in {"", ".", ".."} for part in candidate.parts)
              ):
                  raise SystemExit("Manifest recovery receipt path is unsafe.")
              receipt = manifest.parent / candidate
              if not receipt.is_file() or receipt.is_symlink():
                  raise SystemExit(
                      "Dual-provenance manifest's canonical recovery receipt "
                      "is absent or symlinked."
                  )
              recovery_receipt = receipt.as_posix()
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as out:
              out.write("has_current=true\n")
              out.write(f"recovery_receipt={recovery_receipt}\n")
          print("Current schema-v2 publication evidence detected; strict checks enabled.")
          PY
      - name: Install current-evidence validator dependencies
        if: steps.evidence.outputs.has_current == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install -e "agribrain/backend[dev]"
      - name: Validate current simulation tables
        if: steps.evidence.outputs.has_current == 'true'
        env:
          STRICT_VALIDATION: "1"
        run: |
          python mvp/simulation/validation/validate_results.py
      - name: Verify current manifest bytes and tracked allowlist
        if: steps.evidence.outputs.has_current == 'true'
        env:
          RECOVERY_RECEIPT: ${{ steps.evidence.outputs.recovery_receipt }}
        run: |
          args=(--strict-commit --allow-missing --require-tracked)
          if [[ -n "$RECOVERY_RECEIPT" ]]; then
            args+=(--recovery-receipt "$RECOVERY_RECEIPT")
          fi
          python mvp/simulation/analysis/verify_manifest.py "${args[@]}"

  # Validate the committed publication lock itself. Comparing it with a newly
  # resolved environment would test upstream package churn rather than the
  # source-bound environment used by the evidence run. Dependency upgrades
  # belong in a separate reviewed change with a fresh lock and affected run.
  locked-environment:
    name: committed Python lock integrity
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Install the committed lock
        run: |
          python -m pip install --upgrade pip
          python -m venv .venv-lock
          .venv-lock/bin/python -m pip install --upgrade pip
          .venv-lock/bin/python -m pip install \
            -r agribrain/backend/requirements-lock.txt
          .venv-lock/bin/python -m pip install --no-deps -e agribrain/backend
      - name: Check dependency compatibility and imports
        run: |
          .venv-lock/bin/python -m pip check
          .venv-lock/bin/python -c \
            "import numpy, pandas, scipy, src, pirag; print('locked imports: PASS')"

  python-lint:
    name: python-lint (ruff)
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Install ruff
        run: |
          python -m pip install --upgrade pip
          pip install "ruff>=0.5,<1"
      - name: Run ruff (lint -- structural correctness gate)
        run: |
          # ruff.toml at repo root configures per-file ignores. We gate
          # on the rules that catch real defects rather than style:
          #   E9/F63/F7/F82  -- syntax errors and parse failures
          #   F401          -- unused imports (the review flagged these)
          #   F821          -- undefined names (catches refactor breakage)
          # Style rules (I import-sorting, B bug-prone patterns) are
          # intentionally not yet gated; they should be tightened in a
          # focused cleanup pass before turning them on as build gates.
          ruff check --select E9,F63,F7,F82,F401,F821 \
            agribrain/backend mvp/simulation hpc

  contract-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: agribrain/contracts/hardhat
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install and test
        run: |
          npm ci
          npm test

  frontend-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: agribrain/frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm test
      - name: Build
        run: npm run build

  contract-analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install hardhat deps
        working-directory: agribrain/contracts/hardhat
        run: npm ci
      - name: Run Slither static analysis
        uses: crytic/slither-action@v0.4.0
        with:
          target: agribrain/contracts/hardhat
          slither-args: --exclude-informational --exclude-low
          fail-on: medium
          slither-version: 0.11.4

  # Supply-chain CVE audits. Advisory by design: a new CVE in a
  # transitive dep should not red the build (we cannot patch upstream
  # in real time), but a maintainer pulling up CI logs should see it.
  # Tightening to fail-on-high is the natural next step once the
  # repo is on a regular dep-bump cadence.
  python-audit:
    name: pip-audit (advisory)
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Install pip-audit
        run: |
          python -m pip install --upgrade pip
          pip install pip-audit
      - name: Audit lockfile (advisory)
        run: |
          # --requirement points at the canonical lockfile; --strict
          # would treat any vulnerability as a build failure but is
          # too noisy for a research repo on a non-emergency cadence.
          # Continue-on-error keeps the workflow green; the report is
          # visible in the job log for the maintainer.
          pip-audit --requirement agribrain/backend/requirements-lock.txt \
              --strict 2>&1 | tee /tmp/pip-audit.log || true
          echo "::group::pip-audit summary"
          tail -50 /tmp/pip-audit.log
          echo "::endgroup::"
        continue-on-error: true

  frontend-audit:
    name: npm audit (advisory)
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: agribrain/frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: npm audit (production deps only, advisory)
        run: |
          # --omit=dev limits the surface to runtime deps; dev-only
          # vulnerabilities (eslint plugins, vitest etc.) do not ship
          # to users and are reported separately if needed.
          # Continue-on-error keeps the workflow green; report visible
          # in the job log.
          npm audit --omit=dev --audit-level=high 2>&1 | tee /tmp/npm-audit.log || true
          echo "::group::npm audit summary"
          tail -50 /tmp/npm-audit.log
          echo "::endgroup::"
        continue-on-error: true
